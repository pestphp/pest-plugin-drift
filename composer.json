{
    "name": "pestphp/pestify",
    "description": "The Pestify Project",
    "keywords": [
        "php",
        "framework",
        "pest",
        "unit",
        "test",
        "testing",
        "pestify"
    ],
    "license": "MIT",
    "require": {
        "php": "^8.1.0",
        "nikic/php-parser": "^4.15.4",
        "symfony/console": "^6.2.8",
        "symfony/finder": "^6.2.7"
    },
    "autoload": {
        "psr-4": {
            "Pest\\Pestify\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "require-dev": {
        "pestphp/pest": "^2.3",
        "pestphp/pest-dev-tools": "^2.6.0"
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "config": {
        "sort-packages": true,
        "preferred-install": "dist",
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "bin": [
        "bin/pestify"
    ],
    "scripts": {
        "refacto": "rector",
        "lint": "pint",
        "test:refacto": "rector --dry-run",
        "test:lint": "pint --test",
        "test:types": "phpstan analyse --ansi",
        "test:unit": "pest --colors=always",
        "test": [
            "@test:refacto",
            "@test:lint",
            "@test:types",
            "@test:unit"
        ]
    }
}
